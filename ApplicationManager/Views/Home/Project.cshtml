@model ProjectsModel
@{
    ViewData["Title"] = $"Страница {Model.Name_page}";
}
<div class="navigation-history">
    <p>
        <a asp-action="Index">Главная</a> -> @Model.Name_page
    </p>
</div>
<h1>@Model.Name_page</h1>
<hr />
<div class="boxes-project">
    @foreach (Project_with_image item in Model.Projects)
    {
        <div class="box-project">
            <div class="box-project-border">
                <a asp-action="ProjectDetails" asp-route-id="@item.Id">
                    <div class="ImageProject" style="background-image: url(@item.ImgSrc)"></div>
                    <div class="block-title">
                        <div>@item.Title</div>
                    </div>

                </a>
            </div>

           
        </div>
    }
</div>